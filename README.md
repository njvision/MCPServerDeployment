To access tools just add:
```json
{
  "mcpServers": {
    "add_numbers": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/njvision/MCPServerDeployment.git",
        "mcp-server"
      ]
    }
  }
}
```